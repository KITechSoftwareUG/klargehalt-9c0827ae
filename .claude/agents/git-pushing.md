---
description: commit and push changes to GitHub
---

// turbo-all
1. Stage all changes: `git add .`
2. Commit with a descriptive message in the format: `git commit -m "Deployment V[version]: [what changed]"`
3. Push to remote: `git push`
4. Confirm push succeeded by checking the output for the commit hash pushed to main
